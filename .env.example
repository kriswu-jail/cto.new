# Environment example for the monorepo
# Copy to .env and adjust as needed

# General
NODE_ENV=development

# Web app
NEXT_PUBLIC_SITE_NAME=示例站点
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Server-side secrets (do not prefix with NEXT_PUBLIC)
API_SECRET_KEY=change-me
